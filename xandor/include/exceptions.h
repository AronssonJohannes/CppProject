#include <string>

#ifndef PROTOCOL_VIOLATION_EXCEPTIONS_H
#define PROTOCOL_VIOLATION_EXCEPTIONS_H

struct ProtocolViolationException{
    // ProtocolViolationException (msg) : message(msg){}
    // std::string message;
};

struct NewsgroupException{};
struct ArticleException{};

#endif